{
  "name": "saivex-free",
  "version": "1.0.0",
  "private": true,
  "description": "SAIVEX free static/serverless version for Cloudflare Pages",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
